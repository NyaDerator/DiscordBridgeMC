# Конфигурация Discord бота
discord:
  # Токен бота из Discord Developer Portal
  bot-token: ""

  # ID гильдии (сервера), на котором будет работать бот
  guild-id: ""

  # ID канала для синхронизации чата
  channel-id: ""

  # ID роли, которая разрешает пользователям использовать команды бота и синхронизацию чата
  allowed-role-id: ""

# Особенности плагина
features:
  # Включить синхронизацию чата между Discord и Minecraft
  chat-sync: true

  # Включить сообщения о статусе сервера (вход/выход/смерть)
  server-messages: true

  # Кулдаун (задержка) для команды execute (в миллисекундах)
  execute-command-cooldown: 15000

# Сообщения, отправляемые в Discord
messages:
  # Сообщение о входе игрока
  join: "**{player}** присоединился к серверу"

  # Сообщение о выходе игрока
  leave: "**{player}** покинул сервер"

  # Сообщение о смерти игрока
  death: "💀 **{player}** {message}"
